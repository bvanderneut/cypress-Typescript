export class MyOwnClass {
    /**
     * Add the first plus the second value and returns the sum
     *
     * @param first value one
     * @param second value two
     */
    public static DoSomeMath(first: number, second: number): number {
        return first + second;
    }
}
